# torch/torchvision: this image pins CUDA builds via /etc/pip/constraints.txt
# (torch==2.12.0+cu130). Install them from the PyTorch index first, then the rest:
#   pip install torch==2.12.0+cu130 torchvision==0.27.0+cu130 \
#     --index-url https://download.pytorch.org/whl/cu130
#   pip install -r requirements.txt
# Or use: python3 -m venv --system-site-packages .venv  (reuse image torch).
transformers>=4.40.0
datasets>=2.14.5
accelerate>=0.27.0
safetensors>=0.4.0
timm>=0.9.0
pyyaml>=6.0
scikit-learn>=1.3.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
tqdm>=4.66.0
numpy>=1.24.0
huggingface_hub>=0.20.0
evaluate>=0.4.3
